/**
 * This module contains information used when generating reports on the imported content.
 */
define(
    ['exports'],
    function (exports) {
        'use strict';

        exports.ALLOWED_URLS = [/.*?\.redhat\.com.*?/];
    }
)